import { EmploymentPipePipe } from './employment-pipe.pipe';

describe('EmploymentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmploymentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
